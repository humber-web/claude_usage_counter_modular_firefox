{
  "name": "claude-usage-counter-tests",
  "version": "1.0.0",
  "description": "Test suite for Claude Usage Counter extension",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}